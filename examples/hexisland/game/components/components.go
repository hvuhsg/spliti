// Package components holds the game's component structs. Every exported struct
// here is registered with the editor's inspector by spliti gen. The combat
// components (Enemy, TTL, MuzzleFlash, …) are defined alongside their systems
// in package systems; this package is reserved for components the editor edits.
package components
